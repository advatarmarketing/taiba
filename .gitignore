node_modules
.vercel
.DS_Store

# The local development database. Never committed — it is throwaway test data.
.data/

# Never commit secrets. .env.example is the committed template.
.env
.env.local
.env*.local
.env*
!.env.example
